spring:
  application:
    name: cache-fallback
  datasource:
    url: jdbc:h2:mem:cachefallback
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
  h2:
    console:
      enabled: true
